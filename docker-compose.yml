services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: jlopezsa/sensor-hub-web:0.1.0
    container_name: sensor-hub-web
    env_file:
      - .env
    ports:
      - "3000:3000"
    restart: unless-stopped
